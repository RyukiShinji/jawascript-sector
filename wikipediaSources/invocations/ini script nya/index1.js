"use strict";

// assign the variable into a function
let duplication = function(p) {
    return p 
}

const ret = duplication("konstruksi via variabel dan function, menetapkan function ke variabel");
alert (ret);
